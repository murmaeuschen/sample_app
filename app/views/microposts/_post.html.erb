<li>
  <%= gravatar_for current_user, :size => 30 %>
  <%= link_to current_user.name, current_user %>
  <% if current_user.admin? %>
  | <%= link_to "delete", current_user, :method => :delete, :confirm => "You sure?",
                                :title => "Delete #{user.name}" %>
  <% end %>
</li>

